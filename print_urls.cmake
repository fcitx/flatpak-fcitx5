cmake_minimum_required(VERSION 3.16)

function(fcitx5_download name url file sha256)
  message("${url} ${sha256}")
endfunction()

function(fcitx5_extract)
endfunction()
function(add_custom_command)
endfunction()
function(add_custom_target)
endfunction()
function(install)
endfunction()

set(LIBIME_INSTALL_LIBDATADIR "")
set(LIBIME_INSTALL_PKGDATADIR "")

include("${INPUT_FILE}")
